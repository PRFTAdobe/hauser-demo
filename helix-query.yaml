version: 1

indices:
  pages:
    include:
      - '/**'
    exclude:
      - '/**.json'
    target: /query-index.json
    properties:
      title:
        select: hlx:metadata.title
      description:
        select: hlx:metadata.description
      tags:
        select: hlx:metadata.tags
